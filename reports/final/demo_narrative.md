# Demo Narrative

Use the product demo to show three contrasts:

- `demo_company.revenue` / 월별 신규 고객 순매출을 보여줘: baseline risk = physical-schema baseline may use signup date and gross amount; system behavior = Semantic Pack can use first_paid_at, net revenue formula, join recipe, and policy checks.
- `tableau_superstore` / 지역별 매출과 이익을 보여줘: baseline risk = generic baseline may miss Returns/People workbook semantics; system behavior = file-corpus benchmark records Superstore as a covered sales-order target.

The evidence console must state missing per-file evidence clearly instead of inventing pass rates.
